# MockRelay

Universal local API mock-and-record reverse proxy.

## Install

    pip install -e .

## Use

    mockrelay init
    mockrelay serve
    mockrelay record
    mockrelay replay --latency 150
    mockrelay list
